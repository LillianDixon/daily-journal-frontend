import React, { Component } from 'react';

export default class NewEntry extends Component {
    render() {
        return (
            <div>
                new entry
            </div>
        );
    }
}